name: camera_simple
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mobx: ^1.0.0
  flutter_mobx: ^1.0.0
  camera: ^0.8.1+2
  esys_flutter_share: ^1.0.2
  path: ^1.6.4
  path_provider: ^1.6.11
  image: ^2.1.4
  flutter_modular:
  exif:
  arcore_flutter_plugin:
    path: /home/leonardo/snap/flutter/common/flutter/.pub-cache/hosted/pub.dartlang.org/arcore_flutter_plugin-0.0.11/

dev_dependencies:
  mobx_codegen:
  build_runner:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/r.png
